name: Rust

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Configure
      run: curl https://sh.rustup.rs -sSf | sh -s -- -y
    - name: Install Mac target
      run: rustup target add aarch64-apple-darwin -y
    - name: Install Win target
      run: rustup target add x86_64-pc-windows-gnu -y
    - name: Build Linux
      run: cargo build --verbose
    - name: Build Mac
      run: cargo build --verbose --target aarch64-apple-darwin
    - name: Build Win
      run: cargo build --verbose --target x86_64-pc-windows-gnu
